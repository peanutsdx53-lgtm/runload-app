# RunLoad Record Input UI Prototype V0.4 — Date-row alignment

- Keep native date input behavior.
- Place the date control immediately after the "日付" label.
- Align the overall date row width with the distance / actual-moving-time row.
- Use a low-height underline-style date field so it is visually distinct from the larger numeric measure boxes.
- Keep V0.2 compact distance/time sizing and RPE removal.
- Free-text semantic overlap remains a separate audit item.
