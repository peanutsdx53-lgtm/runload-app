# GPX Local Analysis V0.6
- Removed the redundant "手動で設定" action.
- Returning to manual course entry is unified under the existing top "コース設定へ戻る" route.
- Primary actions are now GPX file selection and sample confirmation.
- Parsing/privacy behavior unchanged.
