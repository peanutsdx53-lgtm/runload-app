# RunLoad Record Input UI Prototype V0.3 — Date-field delta

- Keep the native HTML date input and the iPhone native date picker.
- Change only its presentation on the record screen.
- Mobile date entry is a compact single-row control rather than a full-width large field.
- Constrain the native date control width so WebKit cannot visually overrun the card.
- Right-align the WebKit date value where supported.
- Distance/time sizing and RPE removal from V0.2 are preserved.
- Free-text semantic overlap remains a separate audit item.
