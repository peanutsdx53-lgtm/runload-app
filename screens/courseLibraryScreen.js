import { escapeHtml } from "../ui/commonComponents.js";
import { primarySurfaceSummary, slopeSummary } from "../ui/coursePresentation.js";
import { peekCourseSelection } from "../ui/flowSessionState.js";

function safeReturnTo(context) { const value=String(context?.parameters?.get("returnTo")||"#/record-input"); return ["#/record-input","#/plan","#/simulation"].some((prefix)=>value.startsWith(prefix))?value:"#/record-input"; }
function callerLabel(returnTo="") { if(returnTo.startsWith("#/plan"))return"予定"; if(returnTo.startsWith("#/simulation"))return"条件比較"; return"今日の記録"; }
function selectedPreset(returnTo="") { if(returnTo.startsWith("#/plan"))return peekCourseSelection("plan")?.preset||null; if(returnTo.startsWith("#/simulation"))return peekCourseSelection("simulation")?.preset||null; return null; }
function courseMeta(preset={}) { return `${slopeSummary(preset.course||{})}・${primarySurfaceSummary(preset.course||{})}`; }
function card(preset,returnTo,selectedId="") { return `<article class="card"><div class="card-main"><div class="card-head"><strong>${escapeHtml(preset.name||preset.course?.name||"名称なし")}</strong><span>${preset.id===selectedId?"使用中":"保存済み"}</span></div><div class="facts"><div class="fact"><small>坂道</small><strong>${escapeHtml(slopeSummary(preset.course||{}))}</strong></div><div class="fact"><small>路面</small><strong>${escapeHtml(primarySurfaceSummary(preset.course||{}))}</strong></div></div></div><div class="actions"><button class="use" type="button" data-action="use-course" data-course-id="${escapeHtml(preset.id||"")}" data-return-to="${escapeHtml(returnTo)}">このコースを使う</button><div class="secondary-actions"><a class="edit" href="#/course-editor?id=${encodeURIComponent(preset.id||"")}&returnTo=${encodeURIComponent(returnTo)}">編集</a><button class="delete" type="button" data-action="delete-course" data-course-id="${escapeHtml(preset.id||"")}">削除</button></div></div></article>`; }

export function renderCourseLibraryScreen({ services, context }) {
  const courses=services.storage.courses.loadAll(); const returnTo=safeReturnTo(context); const selected=selectedPreset(returnTo); const selectedId=selected?.id||""; const ordered=[...courses].reverse();
  return `<div class="screen screen--course-library screen-layout screen-layout--course course-derived-screen"><div class="course-derived-frame course-derived-frame--library"><header class="course-derived-head"><a class="course-derived-back" href="${escapeHtml(returnTo)}">← ${escapeHtml(callerLabel(returnTo))}へ戻る</a><strong>コース設定</strong><span aria-hidden="true"></span></header><main class="course-derived-body"><section class="head clean-head"><div><p class="eyebrow">COURSE</p><h1>コース設定</h1><p>今回使うコースを選ぶか、新しく作成します。</p></div><a class="new course-library-pc-only course-library-pc-new" href="#/course-editor?returnTo=${encodeURIComponent(returnTo)}">＋ 新しいコース</a></section><section class="current"><div><small class="course-current-label course-current-label--mobile">現在選択中</small><small class="course-library-pc-only course-current-label course-current-label--pc">今回使うコース</small><strong>${escapeHtml(selected?.name||selected?.course?.name||"未選択")}</strong><span>${escapeHtml(selected?courseMeta(selected):"コースは任意です")}</span></div><b>${selected?"選択中":"未選択"}</b></section><section class="section saved-section"><div class="section-head"><div><small class="course-library-mobile-meta">SAVED COURSES / ${courses.length}件</small><small class="course-library-pc-only course-library-pc-count">保存済み ${courses.length}件</small><h2>保存したコース</h2></div><a class="new course-library-list-new" href="#/course-editor?returnTo=${encodeURIComponent(returnTo)}">＋ 新しいコース</a></div><div class="list">${ordered.length?ordered.map((preset)=>card(preset,returnTo,selectedId)).join(""):'<article class="card empty-course-card"><div class="card-main"><div class="card-head"><strong>保存したコースはまだありません</strong><span>0件</span></div><p>「＋ 新しいコース」から追加できます。</p></div></article>'}</div><p class="course-library-status" data-course-manager-status role="status" aria-live="polite"></p></section><section class="section assist-section"><div class="section-head"><div><small>ASSIST</small><h2>入力を補助する</h2></div></div><div class="import-helper"><div><small>GPX・任意</small><strong>ファイルから坂道を読み取る</strong><span>標高入りGPXを持っている場合だけ使用</span></div><a href="#/gpx-analysis?returnTo=${encodeURIComponent(returnTo)}">GPXを読み込む <i>›</i></a></div></section><p class="boundary">坂や路面が分からない場合は、不明のまま保存できます。</p></main></div></div>`;
}
