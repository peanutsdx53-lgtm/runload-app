name: Codebase cleanup reachability audit

on:
  push:
    branches:
      - codebase-cleanup-20260922
    paths:
      - ".github/workflows/codebase-cleanup-audit.yml"

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Runtime reachability and naming report
        shell: bash
        run: |
          set -euo pipefail
          node <<'NODE'
          const fs=require('fs');
          const path=require('path');
          const root=process.cwd();
          const walk=(dir)=>{
            const out=[];
            for(const ent of fs.readdirSync(dir,{withFileTypes:true})){
              if(ent.name==='.git') continue;
              const p=path.join(dir,ent.name);
              if(ent.isDirectory()) out.push(...walk(p)); else out.push(p);
            }
            return out;
          };
          const rel=p=>path.relative(root,p).replaceAll(path.sep,'/');
          const files=walk(root);
          const js=files.filter(p=>/\.(?:js|mjs)$/.test(p));
          const runtimeJs=js.map(rel).filter(p=>p.endsWith('.js')&&!p.startsWith('tests/'));
          const read=p=>fs.readFileSync(p,'utf8');
          const importRe=/\bimport\s+(?:[^'"]*?\s+from\s+)?["']([^"']+)["']/g;
          const graph=new Map();
          for(const file of js){
            const deps=[];
            const src=read(file);
            for(const m of src.matchAll(importRe)){
              if(!m[1].startsWith('.')) continue;
              let target=path.resolve(path.dirname(file),m[1]);
              if(!path.extname(target)) target+='.js';
              deps.push(rel(target));
            }
            graph.set(rel(file),deps);
          }
          const entries=['app.js','ui/pwaUpdateBootstrap.js'];
          const reachable=new Set();
          const stack=[...entries];
          while(stack.length){
            const cur=stack.pop();
            if(reachable.has(cur)) continue;
            reachable.add(cur);
            for(const dep of graph.get(cur)||[]) stack.push(dep);
          }
          const unreachable=runtimeJs.filter(p=>!reachable.has(p)&&p!=='service-worker.js').sort();

          const html=read(path.join(root,'index.html'));
          const linkedCss=[...html.matchAll(/<link[^>]+href=["']\.\/([^"']+\.css)["']/g)].map(m=>m[1]);
          const css=files.map(rel).filter(p=>p.endsWith('.css'));
          const unlinkedCss=css.filter(p=>!linkedCss.includes(p)).sort();

          const runtimeFlags=[];
          for(const file of files.filter(p=>/\.(?:js|css|html)$/.test(p))){
            const src=read(file);
            for(const [kind,re] of [
              ['prototype',/\bprototype[-_A-Za-z0-9]*/gi],
              ['hotfix',/\bhotfix[-_A-Za-z0-9]*/gi],
              ['preview',/\bpreview[-_A-Za-z0-9]*/gi],
              ['candidate',/\bcandidate\b/gi],
              ['TODO',/\bTODO\b/g],
              ['FIXME',/\bFIXME\b/g],
            ]){
              const matches=[...src.matchAll(re)];
              if(matches.length) runtimeFlags.push({file:rel(file),kind,count:matches.length});
            }
          }

          const sw=read(path.join(root,'service-worker.js'));
          const precache=[...sw.matchAll(/"\.\/([^"]+)"/g)].map(m=>m[1]);
          const missingPrecache=precache.filter(p=>!fs.existsSync(path.join(root,p)));

          console.log('REACHABILITY_RUNTIME_JS='+runtimeJs.length);
          console.log('REACHABILITY_UNREACHABLE='+JSON.stringify(unreachable));
          console.log('REACHABILITY_UNLINKED_CSS='+JSON.stringify(unlinkedCss));
          console.log('REACHABILITY_MISSING_PRECACHE='+JSON.stringify(missingPrecache));
          console.log('REACHABILITY_RUNTIME_FLAGS='+JSON.stringify(runtimeFlags));
          NODE

      - name: Syntax report
        shell: bash
        run: |
          set -euo pipefail
          count=0
          while IFS= read -r file; do
            node --check "$file" >/dev/null
            count=$((count+1))
          done < <(find . -type f \( -name '*.js' -o -name '*.mjs' \) -not -path './.git/*' | sort)
          echo "SYNTAX_FILES_PASS=$count"
